/var/log/nginx/*log {
    create 0644 root root
    daily
    rotate 10
    missingok
    notifempty
    compress
    sharedscripts
    postrotate
        /bin/kill -HUP `cat /run/nginx.pid 2>/dev/null` 2>/dev/null || true
    endscript
}
